{
  "local": {
    "Security": {
      "Auth": {
        "auth-id": {
          "Type": "OAuth2",
          "Grant Type": "Password",
          "Client ID": "{{client_id}}",
          "Username": "{{username}}",
          "Password": "{{password}}",
          "Auth URL": "{{auth_url}}",
          "Token URL": "{{token_url}}",
          "Scope": "openid"
        }
      }
    }
  },
  "staging": {
    "Security": {
      "Auth": {
        "auth-id": {
          "Type": "OAuth2",
          "Grant Type": "Password",
          "Client ID": "{{client_id}}",
          "Username": "{{username}}",
          "Password": "{{password}}",
          "Auth URL": "{{auth_url}}",
          "Token URL": "{{token_url}}",
          "Scope": "openid"
        }
      }
    }
  }
}
